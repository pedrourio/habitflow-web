// src/pages/LoginPage.tsx
import { Typography } from 'antd';
const { Title } = Typography;

export default function LoginPage() {
  return (
    <div>
      <Title level={2}>Página de Login</Title>
      <p>Em breve, aqui teremos nosso formulário de login.</p>
    </div>
  );
}